// The Vercel project is deployed from the repository root. Files in this
// directory are therefore exposed as /api/* serverless routes.
import { app } from '../travel-guide/functions/api.js';

export default app;
